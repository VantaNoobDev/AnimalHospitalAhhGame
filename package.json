{
  "name": "genrojotree",
  "version": "1.0.0",
  "main": "index.js",

  "scripts": {
    "check": "selene src && npm run build:rojo && rojo build -o AnimalHospitalAhhGame.rbxlx && rojo sourcemap default.project.json -o sourcemap.json",
    "test": "npm run check",
    "build:rojo": "node tools/genRojoTree.js",
    "watch:rojo": "chokidar \"src/**/*\" -c \"npm run build:rojo\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "chokidar-cli": "^3.0.0"
  }
}
